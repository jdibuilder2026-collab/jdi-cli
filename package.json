{
  "name": "jdi-cli",
  "version": "0.2.0",
  "description": "JDI — Claude Code를 내장한 AI 코딩 에이전트. 로그인 한 번으로 바로 사용.",
  "type": "module",
  "bin": {
    "jdi": "./dist/bin/jdi.js"
  },
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc --watch",
    "start": "node dist/bin/jdi.js",
    "lint": "npx tsc --noEmit",
    "prepare": "npm run build"
  },
  "keywords": ["cli", "ai", "coding-agent", "claude", "claude-code"],
  "license": "MIT",
  "files": [
    "dist",
    "src",
    "tsconfig.json"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^1.0.0",
    "chalk": "^5.4.1",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
