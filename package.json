{
  "name": "jdi-cli",
  "version": "0.1.0",
  "description": "JDI — AI coding agent CLI. Read, write, and execute code with Claude.",
  "type": "module",
  "bin": {
    "jdi": "./dist/bin/jdi.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/bin/jdi.js",
    "lint": "tsc --noEmit",
    "prepare": "npm run build",
    "postinstall": "npm run build 2>/dev/null || true"
  },
  "keywords": ["cli", "ai", "coding-agent", "claude"],
  "license": "MIT",
  "files": [
    "dist",
    "src",
    "tsconfig.json"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "marked": "^15.0.0",
    "marked-terminal": "^7.3.0",
    "ora": "^8.2.0",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
